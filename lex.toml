[package]
name        = "lex-cli"
version     = "0.1.0"
description = "CLI framework for Lex: argument parsing, ACLI output, persistent config, and an authenticated JSON HTTP client."
license     = "EUPL-1.2"

[dependencies]
lex-schema = { path = "../lex-schema" }
